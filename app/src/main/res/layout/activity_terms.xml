<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen_background"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_height"
        android:background="@android:color/white"
        android:navigationIcon="@drawable/ic_arrow_back"
        android:title="@string/terms_title"
        app:navigationIconTint="@color/card_text"
        app:titleCentered="false"
        app:titleTextAppearance="@style/PlaylistMaker.ToolbarTitle"
        app:titleTextColor="@color/card_text" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/settings_divider" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="@dimen/terms_padding">

        <TextView
            android:id="@+id/termsText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/terms_text"
            android:textColor="@color/card_text"
            android:textSize="@dimen/terms_text_size" />
    </ScrollView>
</LinearLayout>
